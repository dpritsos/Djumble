
import numpy as np
import random as rnd
import scipy.stats as sps

"""
def VonMissesFWinv(x, k):

    return 1 + (1/k * (log(x) + log(1 - (exp(-2 * k) * ((x - 1) / x))))

def VonMissesSampling(mu, k, samples_num):

    von_miss_spls = list()

    for i in range(samples_num):
        rnd.rand

"""


print sps.vonmises.rvs()